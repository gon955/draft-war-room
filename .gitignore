# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/

# Secrets — ESPN_S2/SWID are live session credentials (SPEC §2.3), never commit
.env
.env.*
!.env.example

# Node / Next.js (frontend lands here later)
node_modules/
.next/
out/

# Editor / OS
.vscode/
.idea/
.DS_Store

# one-off ESPN exploration scratch
format_check.py
espn_diag.py
roster_check.py

# Planning and spec documents. Deliberately untracked: they are working notes
# that move faster than the code and are shared as links rather than through
# the repo. Both stay on disk.
Draft_War_Room_SPEC.md
HANDOFF.md

# Calibration cache: pulled from ESPN by scripts/calibration/fetch.py and
# rebuilt on demand, so it is data rather than source.
scripts/calibration/data/
