# Draft War Room — runtime deps (see Draft_War_Room_SPEC.md §1)
fastapi
uvicorn[standard]

# ORM + migrations (§3)
SQLAlchemy>=2.0
alembic

# Models + config (§1)
pydantic>=2
pydantic-settings
email-validator          # required for pydantic EmailStr on users.email

# Postgres driver, JSONB (§1)
psycopg[binary]

# Auth (§1, §4)
PyJWT
passlib[argon2]
python-multipart         # OAuth2 form login body parsing

# Fernet encryption for espn_s2_encrypted (§2.3)
cryptography

# ESPN adapter (§2)
espn-api
